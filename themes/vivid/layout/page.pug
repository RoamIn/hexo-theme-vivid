{% extends 'partial/layout.njk' %}

{% block body %}
    <article>
        <h1>{{ page.title }}</h1>
        <time datetime="{{ page.date }}">{{ date(page.date) }}</time>
        <hr>
        {{ page.content }}
    </article>

    {{ paginator() }}
{% endblock %}
